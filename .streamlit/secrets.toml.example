# Copy this file to secrets.toml (same folder) and fill in your key.
# secrets.toml is gitignored — it never gets committed.
#
# On Streamlit Community Cloud, skip this file entirely: paste the same
# line into your app's App settings -> Secrets instead.

SORARE_API_KEY = "your-sorare-api-key"
